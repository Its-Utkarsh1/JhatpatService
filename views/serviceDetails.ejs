<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= service.name %> | Service Details
  </title>

  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" />

  <style>
    body {
      font-family: Poppins, sans-serif;
      background: #f8f9fa;
      margin: 0;
      padding: 0;
    }

    .header {
      margin-left: 6px;
      margin-right: 6px;
      display: flex;
      align-items: center;
      gap: 10px;
      background: white;
      position: sticky;
      top: 0;
      z-index: 10;
      border-bottom: 1px solid #eee;
    }

    .header i {
      font-size: 1.4rem;
      cursor: pointer;
    }

    .container {
      padding: 16px;
    }

    .main-img {
      width: 100%;
      height: 230px;
      border-radius: 14px;
      object-fit: cover;
    }

    .title {
      font-size: 1.3rem;
      font-weight: 600;
      margin-top: 14px;
    }

    .price-row {
      display: flex;
      align-items: center;
      gap: 8px;
      margin-top: 4px;
    }

    .price {
      font-size: 1.2rem;
      font-weight: 700;
    }

    .strike {
      text-decoration: line-through;
      color: gray;
      font-size: 0.9rem;
    }

    .rating {
      margin-left: auto;
      color: #f4b400;
      font-size: 1rem;
      display: flex;
      align-items: center;
      gap: 4px;
    }

    .action-row {
      margin-top: 18px;
      display: flex;
      justify-content: space-between;
    }

    .action-box {
      background: white;
      padding: 14px 8px;
      flex: 1;
      text-align: center;
      border-radius: 12px;
      border: 1px solid #eee;
      margin: 0 6px;
      cursor: pointer;
    }

    .action-box i {
      font-size: 1.3rem;
      margin-bottom: 5px;
      color: #14213d;
    }

    .provider-card {
      background: white;
      padding: 14px;
      border-radius: 12px;
      display: flex;
      align-items: center;
      gap: 12px;
      border: 1px solid #eee;
      margin-top: 20px;
    }

    .provider-card img {
      width: 50px;
      height: 50px;
      border-radius: 50%;
      object-fit: cover;
    }

    .provider-name {
      font-size: 1rem;
      font-weight: 600;
    }

    .provider-label {
      font-size: 0.8rem;
      color: gray;
      margin-top: -2px;
    }

    .footer {
      position: fixed;
      bottom: 0;
      left: 0;
      right: 0;
      background: white;
      padding: 12px 16px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      border-top: 1px solid #ddd;
    }

    .book-btn {
      background: #14213d;
      color: white;
      padding: 12px 24px;
      border-radius: 10px;
      border: none;
      cursor: pointer;
      font-size: 1rem;
    }
  </style>
</head>

<body>

  <!-- HEADER -->
  <div class="header">
    <i class="bi bi-arrow-left" onclick="history.back()"></i>
    <h3>Service Details</h3>
  </div>

  <!-- MAIN CONTENT -->
  <div class="container">

    <img src="/service/image/<%= service._id %>" class="main-img">

    <div class="title">
      <%= service.name %>
    </div>

    <div class="price-row">
      <span class="price">₹<%= service.price %></span>

      <% if (service.mrp) { %>
        <span class="strike">₹<%= service.mrp %></span>
        <% } %>

    </div>

    <!-- ACTION BUTTONS -->
    <div class="action-row">
<div class="action-box" onclick="location.href='tel:<%= service.phone %>'">
  <i class="bi bi-telephone"></i><br>Call
</div>
      <!-- <div class="action-box"><i class="bi bi-chat-dots"></i><br>Chat</div>
      <div class="action-box"><i class="bi bi-geo-alt"></i><br>Map</div>
      <div class="action-box"><i class="bi bi-share"></i><br>Share</div> -->
    </div>

    <!-- PROVIDER -->
    <div class="section-title" style="margin-top:20px; font-weight:600;">About Service Provider</div>

    <div class="provider-card">
      <img src="<%= service.providerPic ? '/user/profilePic/' + service.providerId : '/assets/default.jpg' %>"
        onerror="this.src='/assets/default-user.png'" class="provider-img" />

      <div>
        <div class="provider-name">
          <%= service.providerName %>
        </div>
        <div class="provider-label">Service Provider</div>
      </div>
    </div>

  </div>

  <!-- FOOTER BOOK BUTTON -->
  <div class="footer">
    <span style="font-size:1.2rem; font-weight:600;">₹<%= service.price %></span>

    <% if (user && user.role==="user" ) { %>
      <form action="/booking/create" method="POST">
        <input type="hidden" name="serviceId" value="<%= service._id %>">
        <input type="hidden" name="date" value="<%= new Date().toISOString().split('T')[0] %>">
        <input type="hidden" name="time" value="ASAP">

        <button class="book-btn">Book Service</button>
      </form>
      <% } %>
        <!-- If USER is provider -->
        <% if (user && user.role==="provider" ) { %>
          <form action="/service/delete/<%= service._id %>" method="POST"
            onsubmit="return confirm('Are you sure you want to delete this service?')">
            <button class="book-btn" style="background:#d9534f;">Delete</button>
          </form>
          <% } %>


  </div>

</body>

</html>
