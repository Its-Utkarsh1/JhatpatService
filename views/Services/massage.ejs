<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Laundry Services | Jhatpat</title>

  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" />

  <style>
    body {
      font-family: "Poppins", sans-serif;
      background: #f8f9fa;
      padding-bottom: 80px;
      margin: 0;
    }

    /* HEADER */
    .header {
      position: sticky;
      top: 0;
      background: #fff;
      padding: 12px 18px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      box-shadow: 0px 1px 5px rgba(0, 0, 0, 0.1);
      z-index: 20;
    }

    .header h3 {
      margin: 0;
      font-size: 1.2rem;
      font-weight: 600;
    }

    .admin-btn {
      background: #14213d;
      color: white;
      padding: 6px 12px;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      font-size: 0.9rem;
    }

    /* SEARCH BAR */
    .search-container {
      padding: 12px 16px;
      display: flex;
      gap: 10px;
    }

    .search-container input {
      flex: 1;
      padding: 10px;
      border-radius: 8px;
      border: 1px solid #ccc;
      font-size: 0.95rem;
    }

    .search-container button {
      background: #14213d;
      border: none;
      padding: 10px 14px;
      border-radius: 8px;
      cursor: pointer;
      color: white;
    }

    /* SERVICE LIST */
    #serviceList {
      padding: 16px;
      display: grid;
      gap: 20px;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    }

    .service-card {
      background: #fff;
      border-radius: 16px;
      overflow: hidden;
      box-shadow: 0px 4px 12px rgba(0, 0, 0, 0.08);
      transition: 0.2s ease;
      height: 330px;
      display: flex;
      flex-direction: column;
    }

    .service-card img {
      width: 100%;
      height: 150px;
      object-fit: cover;
    }

    .service-content {
      padding: 14px;
      display: flex;
      flex-direction: column;
      flex: 1;
    }

    .service-title {
      font-size: 1.05rem;
      font-weight: 600;
      margin-bottom: 5px;
      height: 44px;
      overflow: hidden;
    }

    .price-row {
      display: flex;
      gap: 8px;
      align-items: center;
    }

    .price {
      font-weight: 700;
      font-size: 1rem;
      color: #14213d;
    }

    .strike {
      font-size: 0.85rem;
      text-decoration: line-through;
      color: gray;
    }

    .bottom-row {
      margin-top: auto;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .provider-name {
      margin-bottom: 3px;
      font-size: 0.9rem;
      font-weight: 600;
    }

    .provider-label {
      font-size: 0.75rem;
      color: gray;
      margin-top: -3px;
    }

    .add-btn {
      background: #14213d;
      color: white;
      padding: 6px 14px;
      border-radius: 8px;
      font-size: 0.9rem;
      border: none;
      cursor: pointer;
    }

    /* MODAL */
    .overlay {
      position: fixed;
      inset: 0;
      display: none;
      justify-content: center;
      align-items: center;
      background: rgba(0, 0, 0, 0.45);
      backdrop-filter: blur(4px);
      z-index: 1000;
    }

    .modal {
      background: white;
      padding: 20px;
      width: 90%;
      max-width: 400px;
      border-radius: 16px;
    }

    .modal input,
    .modal select {
      width: 100%;
      padding: 10px;
      margin-bottom: 12px;
      border-radius: 8px;
      border: 1px solid #ccc;
    }

    .modal-btns {
      display: flex;
      justify-content: flex-end;
      gap: 10px;
    }

    .cancel-btn {
      background: #ccc;
      padding: 8px 14px;
      border-radius: 8px;
      border: none;
      cursor: pointer;
    }

    .save-btn {
      background: #14213d;
      color: white;
      padding: 8px 14px;
      border-radius: 8px;
      border: none;
      cursor: pointer;
    }
  </style>

</head>

<body>

  <!-- HEADER -->
  <div class="header">
    <i class="bi bi-arrow-left" onclick="history.back()" style="font-size:1.4rem;"></i>
    <h3>Laundry Services</h3>

    <% if (user && user.role==="provider" ) { %>
      <button class="admin-btn" onclick="document.getElementById('modal').style.display='flex'">+ Add</button>
      <% } %>
  </div>

  <!-- SEARCH BAR -->
  <div class="search-container">
    <input type="text" id="searchInput" placeholder="Search laundry services..." />
    <button id="searchBtn"><i class="bi bi-search"></i></button>
  </div>

  <!-- SERVICE LIST -->
  <div id="serviceList">

    <% if (!services || services.length===0) { %>
      <p style="text-align:center;color:gray;width:100%">No Laundry Services Available</p>
      <% } %>

        <% services.forEach(svc=> { %>

          <div class="service-card">

            <div onclick="window.location.href='/service/details/<%= svc._id %>'">
              <img src="/service/image/<%= svc._id %>" />
            </div>

            <div class="service-content">

              <h3 class="service-title" onclick="window.location.href='/service/details/<%= svc._id %>'"
                style="cursor:pointer;">
                <%= svc.name %>
              </h3>

              <div class="price-row">
                <span class="price">₹<%= svc.price %></span>
                <% if (svc.mrp) { %>
                  <span class="strike">₹<%= svc.mrp %></span>
                  <% } %>
              </div>

              <div class="bottom-row">
                <div>
                  <p class="provider-name">
                    <%= svc.providerName %>
                  </p>
                  <p class="provider-label">Service Provider</p>
                </div>

                <!-- If USER is customer -->
                <% if (user && user.role==="user" ) { %>
                  <form action="/booking/create" method="POST">
                    <input type="hidden" name="serviceId" value="<%= svc._id %>">
                    <input type="hidden" name="date" value="<%= new Date().toISOString().split('T')[0] %>">
                    <input type="hidden" name="time" value="ASAP">
                    <button class="add-btn">Add</button>
                  </form>
                  <% } %>

                    <!-- If USER is provider -->
                    <% if (user && user.role==="provider" ) { %>
                      <form action="/service/delete/<%= svc._id %>" method="POST"
                        onsubmit="return confirm('Are you sure you want to delete this service?')">
                        <button class="add-btn" style="background:#d9534f;">Delete</button>
                      </form>
                      <% } %>

              </div>

            </div>

          </div>

          <% }) %>

  </div>

  <!-- ADD LAUNDRY SERVICE MODAL -->
  <div class="overlay" id="modal">
    <form method="POST" action="/service/create" enctype="multipart/form-data" class="modal">

      <h3 style="text-align:center;margin-bottom:10px;">Add Laundry Service</h3>

      <input type="hidden" name="mainCategory" value="laundry">

      <label>Service Name</label>
      <select name="name" required>
        <option value="">Select Laundry Service</option>
        <option value="Washing & Folding">Washing & Folding</option>
        <option value="Washing & Ironing">Washing & Ironing</option>
        <option value="Dry Cleaning">Dry Cleaning</option>
        <option value="Carpet Laundry">Carpet Laundry</option>
        <option value="Curtain Laundry">Curtain Laundry</option>
        <option value="Blanket & Quilt Laundry">Blanket & Quilt Laundry</option>
        <option value="Shoe Laundry">Shoe Laundry</option>
        <option value="Bag Cleaning">Bag Cleaning</option>
        <option value="Uniform Cleaning">Uniform Cleaning</option>
      </select>

      <label>Original Price</label>
      <input type="number" name="price" required />

      <label>Discounted Price</label>
      <input type="number" name="mrp" placeholder="(₹)" />

      <label>Service Image</label>
      <input type="file" name="image" accept="image/*" required />

      <div class="modal-btns">
        <button type="button" class="cancel-btn"
          onclick="document.getElementById('modal').style.display='none'">Cancel</button>
        <button type="submit" class="save-btn">Save</button>
      </div>

    </form>
  </div>

  <script>
    document.getElementById("searchBtn").addEventListener("click", () => {
      const q = document.getElementById("searchInput").value.toLowerCase();
      const cards = document.querySelectorAll(".service-card");

      cards.forEach(card => {
        const title = card.querySelector(".service-title").innerText.toLowerCase();
        const provider = card.querySelector(".provider-name").innerText.toLowerCase();

        card.style.display = (title.includes(q) || provider.includes(q)) ? "block" : "none";
      });
    });
  </script>

</body>

</html>